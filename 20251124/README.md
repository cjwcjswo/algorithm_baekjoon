# 2025년 11월 24일 문제 풀이 정리

## 📚 학습 내용 요약

### 1. 기본 입출력 및 사칙연산

#### 문제: 2557, 1000, 1001, 10998, 1008, 10869
- **핵심 문법**: `input()`, `split()`, `map()`, `int()`
- **학습 포인트**:
  - `input().split()`: 공백으로 구분된 입력 받기
  - `map(int, input().split())`: 여러 정수를 한 번에 변환
  - `sum(map(int, parse_values))`: 리스트 합계 구하기

**개선된 코드 예시**:
```python
# 1000번 문제
a, b = map(int, input().split())
print(a + b)

# 10869번 문제
a, b = map(int, input().split())
print(a + b)
print(a - b)
print(a * b)
print(a // b)  # 정수 나눗셈
print(a % b)
```

### 2. 조건문 (if-elif-else)

#### 문제: 1330, 9498, 2753, 14681, 2884, 2480
- **핵심 문법**: 조건문, 논리 연산자, 중첩 조건문
- **학습 포인트**:
  - 비교 연산자: `>`, `<`, `==`, `>=`, `<=`
  - 논리 연산자: `and`, `or`, `not`
  - `elif`를 활용한 다중 조건 처리

**개선된 코드 예시**:
```python
# 2753번 (윤년 판별) - 더 간결한 버전
year = int(input())
if (year % 4 == 0 and year % 100 != 0) or (year % 400 == 0):
    print(1)
else:
    print(0)

# 14681번 (사분면 판별) - 더 간결한 버전
x, y = int(input()), int(input())
if x > 0 and y > 0:
    print(1)
elif x < 0 and y > 0:
    print(2)
elif x < 0 and y < 0:
    print(3)
else:
    print(4)

# 2480번 (주사위) - 더 효율적인 버전
a, b, c = map(int, input().split())
if a == b == c:
    print(10000 + a * 1000)
elif a == b or a == c:
    print(1000 + a * 100)
elif b == c:
    print(1000 + b * 100)
else:
    print(max(a, b, c) * 100)
```

### 3. 반복문 (for, while)

#### 문제: 2739, 8393, 10950, 10952, 10951, 15552, 11021, 11022
- **핵심 문법**: `for`, `range()`, `while`, `sys.stdin.readline()`
- **학습 포인트**:
  - `range(1, n+1)`: 1부터 n까지 반복
  - `range(n)`: 0부터 n-1까지 반복
  - `sys.stdin.readline()`: 빠른 입력 (대용량 데이터 처리)
  - `rstrip()`: 줄바꿈 문자 제거

**개선된 코드 예시**:
```python
# 15552번 - 빠른 입출력
import sys
input = sys.stdin.readline  # 함수 재정의로 간결하게

t = int(input())
for _ in range(t):
    a, b = map(int, input().split())
    print(a + b)

# 11021번 - f-string 활용
import sys
t = int(sys.stdin.readline())
for i in range(1, t + 1):
    a, b = map(int, sys.stdin.readline().split())
    print(f"Case #{i}: {a + b}")
```

### 4. 문자열 처리

#### 문제: 10926, 10171, 10172
- **핵심 문법**: 문자열 연결, 이스케이프 문자, 삼중 따옴표
- **학습 포인트**:
  - `+` 연산자로 문자열 연결
  - `'''` 또는 `"""`로 여러 줄 문자열 표현
  - 이스케이프 문자: `\\`, `\"`, `\'`

### 5. 반복문과 문자열 조작

#### 문제: 2438, 2439, 25314
- **핵심 문법**: 문자열 반복, 슬라이싱, f-string
- **학습 포인트**:
  - `'*' * n`: 문자열 반복
  - `' ' * (n-i)`: 공백 반복
  - f-string 포맷팅: `f"{변수}"`

**개선된 코드 예시**:
```python
# 2438번 - 더 간결한 버전
n = int(input())
for i in range(1, n + 1):
    print('*' * i)

# 2439번 - 더 간결한 버전
n = int(input())
for i in range(1, n + 1):
    print(' ' * (n - i) + '*' * i)

# 25314번 - 더 간결한 버전
n = int(input())
print('long ' * (n // 4) + 'int')
```

### 6. 수학 연산 및 검증

#### 문제: 25304, 10430, 18108
- **핵심 문법**: 반복문을 통한 합계 계산, 나머지 연산
- **학습 포인트**:
  - 반복문으로 누적 합계 계산
  - `%` 연산자 활용

**개선된 코드 예시**:
```python
# 25304번 - 더 간결한 버전
total = int(input())
n = int(input())
sum = 0
for _ in range(n):
    price, count = map(int, input().split())
    sum += price * count
print("Yes" if total == sum else "No")
```

## 🎯 핵심 파이썬 문법 정리

1. **입력 받기**:
   - `input()`: 한 줄 입력
   - `map(int, input().split())`: 여러 정수 입력
   - `sys.stdin.readline()`: 빠른 입력

2. **조건문**:
   - `if-elif-else` 구조
   - 중첩 조건문보다 논리 연산자 활용 권장

3. **반복문**:
   - `for i in range(n)`: n번 반복
   - `for i in range(1, n+1)`: 1부터 n까지
   - `while True:`: 무한 루프

4. **문자열**:
   - `'*' * n`: 문자열 반복
   - f-string: `f"{변수}"`
   - 문자열 슬라이싱: `str[start:end]`

5. **리스트**:
   - `list(map(int, input().split()))`: 정수 리스트 입력
   - `append()`: 요소 추가

## 💡 효율성 개선 팁

1. **빠른 입출력**: 대량의 입력이 필요할 때는 `sys.stdin.readline()` 사용
2. **조건문 간소화**: 중첩 if보다 논리 연산자(`and`, `or`) 활용
3. **문자열 반복**: `'문자' * n` 형태로 간결하게 표현
4. **삼항 연산자**: `"Yes" if condition else "No"` 형태로 간결하게

